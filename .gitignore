# Built by `pip install -e .` / `pipx install -e .` — do not commit
*.egg-info/
__pycache__/
*.py[cod]

venv/
.venv/
dist/
build/

# Per-user persistent context (created on first run; do not commit)
memory.json

# Vector index (local, rebuilt per machine)
.cogem/
